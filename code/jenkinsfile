pipeline{
    agent any
    stages{
        stage('Git-Checkout'){
            steps{
                git url: 'https://github.com/niranjansc/devops.git',branch:'main'
            }
        }
        stage('Build'){
            steps{
                sh '''
                   printenv 
                '''
            }
        }
    }
}